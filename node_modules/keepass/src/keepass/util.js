'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});
var boolXml = function boolXml(v) {
  if (typeof v === 'boolean') return v ? 'True' : 'False';

  return v === 'True';
};

exports.boolXml = boolXml;
var makeEnum = function makeEnum(arr) {
  var res = {};
  arr.forEach(function (a, idx) {
    return res[a] = idx;
  });
  Object.defineProperty(res, 'length', {
    value: arr.length,
    enumerable: false
  });
  return res;
};

exports.makeEnum = makeEnum;
var arrNode = function arrNode(json) {
  if (Array.isArray(json)) return json;
  if (json) return [json];

  return [];
};
exports.arrNode = arrNode;