'use strict';

Object.defineProperty(exports, '__esModule', {
  value: true
});

var _buffer = require('buffer');

var errors = {
  typeOf: function typeOf(arg, type, name) {
    if (typeof arg !== type) {
      throw new Error('Expected `' + name + '` to be a ' + type);
    }
  },

  instanceOf: function instanceOf(arg, type, name) {
    if (!(arg instanceof type)) {
      throw new Error('Expected `' + name + '` to implement ' + type.name);
    }
  },

  buffer: function buffer(arg, name) {
    if (!_buffer.Buffer.isBuffer(arg)) {
      throw new Error('Expected `' + name + '` to be buffer');
    }
  }
};

exports['default'] = errors;
module.exports = exports['default'];